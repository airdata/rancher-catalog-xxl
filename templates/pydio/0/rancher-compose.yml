.catalog:
  name: "Pydio"
  version: "v1.1"
  description: "Pydio for Secure File Sharing for business"
  minimum_rancher_version: v0.46.0
  questions:
    - variable: "linkconfig"
      type: string
      required: true
      label: "Link Config"
      description: "volumes for config"
    - variable: "linkdata"
      type: string
      required: true
      label: "Link Data"
      description: "volumes for data"
    - variable: "rootpass"
      type: string
      required: true
      label: "root password for database"
      description: "root password for database"
    - variable: "user"
      type: string
      required: true
      label: "userpass"
      description: "user for database"
    - variable: "userpass"
      type: string
      required: true
      label: "root password for database"
      description: "root password for database"
    - variable: "dbname"
      type: string
      required: true
      label: "name for database"
      description: "name for database"
