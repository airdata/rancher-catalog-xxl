version: '2'

services:
  wordpress:
    image: wordpress
    environment:
      WORDPRESS_DB_PASSWORD: example
  mysql:
    image: mariadb
    environment:
      MYSQL_ROOT_PASSWORD: example
  wordpress-lb:
    image: rancher/none
