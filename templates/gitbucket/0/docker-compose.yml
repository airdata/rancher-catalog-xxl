version: '2'
services:
  gitbucket:
    image: hiono/gitbucket
    stdin_open: true
    volumes:
      - ${EDX_DATA_ROOT}:/bitbucket
    tty: true
    ports:
      - ${EXPOSED_SSH_PORT}:29418/tcp
      - ${EXPOSED_WEB_PORT}:8080/tcp
